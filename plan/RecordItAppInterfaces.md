# User Stories
1. As a user I would like to download a binary file, a ready dmg file instead of pulling repository, make build, run the program via the command line.
2. As a user, install the dmg file should by default download all neccesary libraries and components.
3.
